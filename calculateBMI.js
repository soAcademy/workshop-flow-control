const calculateBMI = (weight, height) => {
  const bmi = weight / (height / 100) ** 2;
  let grade = '';
  // To do: write the condition here
  // if (condition) { do something }
  console.log('Your BMI grade is: ', grade);
}

calculateBMI(55, 163)