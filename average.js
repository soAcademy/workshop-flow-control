const average = (input) => {
    let sum = 0;
    let average = 0;
    for (let i = 0; i < input.length; i++) {
        // To do: write the sum of input here
        // You can get input at each index by input[i]
    }
    // To do: write the calculation of average here
    // average = ?
    console.log('The average is: ', average);
 }
 
 average([1, 2, 3, 4])